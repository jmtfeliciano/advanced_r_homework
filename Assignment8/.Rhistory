data1[(data1$id==3 & data1$name=="Zen"),]
data1[(data1$id==3 & data1$name=="Zen"),]
TRUE && FALSE
TRUE & FALSE
data1[data1$id==3 & data1$name=="Zen",]
?print
print(c(1,2,))
print(c(1,2,3))
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
for(row in 1:nrow(test_makeup)) {
if(test_original$StudentID = test_makeup$StudentID & test_original$TestScore){
print(test_original)
}
# test_original[test_original$StudentID = ]
}
for(row in 1:nrow(test_makeup)) {
if(test_original$StudentID = test_makeup$StudentID & test_original$Number == test_makeup$TestNumber){
print(test_original)
}
# test_original[test_original$StudentID = ]
}
row_stacked_data <- rbind(test_original, test_makeup)
n_occur <- data.frame(table(test_original$StudentID))
n_occur
row_stacked_data <- rbind(test_original, test_makeup)
wide.fulldata<-reshape(long.fulldata1, timevar="time", idvar="TagID", v.names="PST", direction="wide")
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
row_stacked_data <- rbind(test_original, test_makeup)
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
sorted_row_stacked <- row_stacked_data[order(StudentID, TestNumber),]
head(row_stacked_data)
row_stacked_data <- rbind(test_original, test_makeup)
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
head(test_original)
sorted_row_stacked <- row_stacked_data[order(StudentID, TestNumber),]
sorted_row_stacked <- row_stacked_data[order(StudentID),]
sorted_row_stacked <- row_stacked_data[order(row_stacked_data$StudentID, row_stacked_data$TestNumber),]
head(sorted_row_stacked)
wide_data <- reshape(sorted_row_stacked, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
n_occur
n_occur[n_occur$Freq > 5,]
n_occur[n_occur$Freq > 5,]
reshape(sorted_row_stacked, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
View(sorted_row_stacked)
test_makeup
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
test_original[is.na(test_original$TestScore) == FALSE,]
is.na(test_original$TestScore)
is.na(test_original$TestScore) == TRUE
!is.na(test_original$TestScore)
nrow(test_original)
test_original <- test_original[is.na(test_original$TestScore) == FALSE,]
nrow(test_original)
row_stacked_data <- rbind(test_original, test_makeup)
wide_data <- reshape(sorted_row_stacked, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
test_original <- test_original[is.na(test_original$TestScore) == FALSE,]
row_stacked_data <- rbind(test_original, test_makeup)
sorted_row_stacked <- row_stacked_data[order(row_stacked_data$StudentID, row_stacked_data$TestNumber),]
wide_data <- reshape(row_stacked, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide_data <- reshape(test_original, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide_data
test_original <- test_original[!is.na(test_original$TestScore),]
nrow(test_original)
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the
test_original <- test_original[!is.na(test_original$TestScore),]
nrow(test_original)
reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide_data
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore ", direction="wide")
head(demo_data)
full_data <- merge(demo_data, wide_data, by="StudentID")
full_data
full_data <- merge(demo_data, wide_data, by="StudentID")
full.data
full_data
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide.data
wide_data
full_data <- merge(demo_data, wide_data, by="StudentID", all= TRUE)
full_data
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
?wide_data
head(wide_data)
wide_data <- wide_data %>% mutate(test_avg = mean(TestScore.1:5))
wide_data <- wide_data %>% mutate(test_avg = mean(TestScore[1:5]))
wide_data <- wide_data %>% mutate(test_avg = mean(TestScore.[1:5]))
wide_data <- wide_data %>% mutate(test_avg = mean(c(1,2)))
head(wide_data)
wide_data <- wide_data %>% mutate(test_avg = mean(TestScore.1, TestScore.2))
wide_data <- wide_data %>% mutate(test_avg = mean(c(TestScore.1, TestScore.2))
full_data <- merge(demo_data, wide_data, by="StudentID", all= TRUE)
wide_data <- wide_data %>% mutate(test_avg = mean(c(TestScore.1, TestScore.2)))
wide_data
?mean
row_stacked_data <- rbind(test_original, test_makeup)
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
wide_data
tail(wide_data)
wide_data <- wide_data %>% replace_na(0)
wide_data[1,1]
wide_data[1,2]
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.NA(wide_data[row,col])){
wide_data[row,col] = 0
}
}
}
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] = 0
}
}
}
tail(wide_data)
?mean
mean_grade <- mean(wide_data[row,2:6])
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
mean_grade <- mean(wide_data[row,2:6])
}
}
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
wide_data$mean_grade <- mean(wide_data[row,2:6])
}
}
tail(wide_data)
wide_data[1,2:6]
mean( wide_data[1,2:6])
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
sum(1,23)
t.1 <- 2
t.2 <- 3
t.[1:2]
t[1:2]
t.c(1,2)
?sum
wide_data <- wide_data %>% mutate(test_avg = sum(TestScore.1,TestScore.2,TestScore.3,TestScore.4,TestScore.5)/5)
wide_data
row_stacked_data <- rbind(test_original, test_makeup)
row_stacked_data <- rbind(test_original, test_makeup)
wide_data <- wide_data %>% mutate(test_avg = sum(TestScore.1,TestScore.2,TestScore.3,TestScore.4,TestScore.5, na.rm=TRUE)/5)
head(wide_data)
wide_data <- wide_data %>% mutate(test_avg = sum(TestScore.1,TestScore.2,TestScore.3,TestScore.4,TestScore.5, na.rm=TRUE)/5.0)
head(wide_data)
wide_data <- wide_data %>% mutate(test_avg = (TestScore.1+TestScore.2+TestScore.3+TestScore.4+TestScore.5)/5.0)
head(wide_data)
tail(wide_data)
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (TestScore.1+TestScore.2+TestScore.3+TestScore.4+TestScore.5)/5.0)
}
}
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (TestScore.1+TestScore.2+TestScore.3+TestScore.4+TestScore.5)/5.0
}
}
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (wide_data$TestScore.1+wide_data$TestScore.2+wide_data$TestScore.3+wide_data$TestScore.4+wide_data$TestScore.5)/5.0
}
}
;
for(row in 1:nrow(wide_data)){
for(col in 2:6){
if(is.na(wide_data[row,col])){
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (wide_data$TestScore.1+wide_data$TestScore.2+wide_data$TestScore.3+wide_data$TestScore.4+wide_data$TestScore.5)/5.0
}
}
k
wide_data
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (wide_data$TestScore.1+wide_data$TestScore.2+wide_data$TestScore.3+wide_data$TestScore.4+wide_data$TestScore.5)/5.0
}
}
k
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (wide_data$TestScore.1+wide_data$TestScore.2+wide_data$TestScore.3+wide_data$TestScore.4+wide_data$TestScore.5)/5
}
}
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- (wide_data$TestScore.1+wide_data$TestScore.2+wide_data$TestScore.3+wide_data$TestScore.4+wide_data$TestScore.5)/5
}
}
"test" + c(1:4)
test <- data.frame(a=(1,2,3,4),b=(2,3,4,5),c=(6,7,8,9))
test <- data.frame(a=(1,2,3,4),b=(2,3,4,5),c=(6,7,8,9))
test <- data.frame( a=(1,2,3,4), b=(2,3,4,5), c=(6,7,8,9) )
test <- data.frame( a=c(1,2,3,4), b=c(2,3,4,5), c=c(6,7,8,9) )
test[1]
test[1,]
?rowSums
wide_data$mean_grade <- rowSums(row,2:6,na.rm=TRUE)/5
wide_data$mean_grade <- rowSums(wide_data[row,2:6],na.rm=TRUE)/5
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- rowSums(wide_data[row,2:6],na.rm=TRUE)/5
}
}
wide_data
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- rowSum(wide_data[row,2:6],na.rm=TRUE)/5
}
}
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- colSum(wide_data[row,2:6],na.rm=TRUE)/5
}
}
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- colSums(wide_data[row,2:6],na.rm=TRUE)/5
}
}
colSums()
head(wide_data)
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- colSums(wide_data[row,2:6],na.rm=TRUE)/5
}
}
head(wide_data)
wide_data$mean_grade <- colSums(wide_data[,2:6],na.rm=TRUE)/5
wide_data
wide_data$mean_grade <- colSums(wide_data[,2:6],na.rm=TRUE) / 5
wide_data[,2:6]
wide_data[1,2:6]
mean(wide_data[1,2:6])
mean(as.vector(wide_data[1,2:6]))
mean(as.matrix(wide_data[1,2:6]))
sum(wide_data[1,2:6])
wide_data[1,2:6]/5
as.matrix(wide_data[1,2:6])/5
sum(wide_data[1,2:6])/5
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- sum(wide_data[row,2:6],na.rm=TRUE)/5
}
}
wide_data
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
wide_data$mean_grade <- sum(wide_data[row,2:6],na.rm=TRUE)/5
}
}
wide_data
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
}
wide_data$mean_grade <- sum(wide_data[row,2:6],na.rm=TRUE)/5
}
wide_data
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
}
wide_data$mean_grade <- sum(wide_data[row,2:6],na.rm=TRUE)/5
}
wide_data
# Josemari Feliciano
# HW 8
# Declares a changeable wd and sets working directory
wd<-"/Users/jfeliciano/Documents/advancedr/homework/Assignment8"
setwd(wd)
# Loads the file required for this homework
demo_data <- read.csv("Demographics.csv", header = TRUE)
test_original <- read.csv("TestScores_original.csv", header = TRUE)
test_makeup <- read.csv("TestScores_makeup.csv", header = TRUE)
# removes all the NA test scores -- this will prevent collision when merging combined data into wide format later
test_original <- test_original[!is.na(test_original$TestScore),]
# Stacks my data on top of one another
row_stacked_data <- rbind(test_original, test_makeup)
# Transforms data from long to wide
wide_data <- reshape(row_stacked_data, timevar="TestNumber", idvar = "StudentID", v.names = "TestScore", direction="wide")
# For-loop goes row by row, repl
for(row in 1:nrow(wide_data)) {
for(col in 2:6) {
if(is.na(wide_data[row,col])) {
wide_data[row,col] <- 0
}
}
}
wide_data$mean_grade <- sum(wide_data[,2:6],na.rm=TRUE)/5
wode_data
wide_data
sum(wide_data[,2:6],na.rm=TRUE)/5
sum(wide_data)
